## <% if (section.name){ %>[<%=section.name%>](<%=section.url%>).<%}%><%=table.name%>

<%=table.desc%>
<% if(custom) {%><%=custom%><%}%>

### Structure
<%- include("./_props.md.ejs", {section, name: table.name, props, table:false});  %>

<% if(funcs.length || selfFuncs.length) { %> #### Functions
<%- include("./_funcs.md.ejs", {parentName:`${table.name}.`, funcs: selfFuncs}); %>
<%- include("./_funcs.md.ejs", {parentName:`${table.name}.`, funcs}); %>
<% } %>

<% if (section.name){ %>
[< Back to `<%=section.name%>`](<%=section.url%>)
<%} else {%>
[< Back to `SLua`](index.html)
<%}%>