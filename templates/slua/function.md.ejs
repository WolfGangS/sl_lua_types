## <% if (section.name){ %>[<%=section.name%>](<%=section.url%>).<%}%><%=func.name%>
<% if(definitions.length > 1) {%>
### `<% if (section.name){ %><%=section.name%>.<%}%><%=func.name%>`
<% for(const definition of definitions) {%>
 - `<%-definition%>`
<% } %>
<% } else { %>
### `<% if (section.name){ %><%=section.name%>.<%}%><%=func.name%>` `<%-definitions[0]%>`
<% } %>

<%=func.desc%>

<% if(alternatives) { %>
<%=alternatives%>

<% } %>
<% if(custom) { %>
<%-custom%>
<%} else {%>
### Example

```lua
<%-example%>
```

<%}%>
<% if(Object.entries(externalLinks).length) {%>
### External Links
<% for(const entry of Object.entries(externalLinks)) { %>
 - [<%= entry[0] %>](<%= entry[1] %>)
<% } %>

<% } %>

<details>
<summary>Raw function spec json</summary>

```json
<%-JSON.stringify(func,null,2)%>
```

</details>

<% if (section.name){ %>
[< Back to `<%=section.name%>`](<%=section.url%>)
<%} else {%>
[< Back to `SLua`](../index.html)
<%}%>